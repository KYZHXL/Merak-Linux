# 核心依赖
pyyaml>=6.0
httpx>=0.27
flask>=3.0
websockets>=13.0

# 已装（引擎/本地 embedding 依赖）
# torch>=2.0, transformers>=5.0, numpy

# 可选：桌面窗口（缺省时自动降级为浏览器）
# pywebview>=5.0

# 可选：TUI 已随 rich 提供（已装 15.0.0）
# rich>=13.0
